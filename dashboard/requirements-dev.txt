# Build/dev-only tools — NOT needed to run Regalia.
pyinstaller>=6.0   # packages desktop.py into a standalone app (regalia.spec)
pillow>=10.0       # lets PyInstaller convert Assets/Regalia_Icon.png into platform icons
