flask>=3.0.0
pyyaml>=6.0.1
anthropic>=0.40.0
pywebview>=5.0  # desktop app shell (desktop.py); browser mode (app.py) needs only the above

# Email (Inbox view + email tools). Auth-only libs — the OAuth/token dance is the
# hard, security-sensitive part we don't hand-roll. The actual mail I/O (Gmail API
# + Microsoft Graph) is plain urllib+json in mailbox.py, no extra SDK. Only needed
# if you connect an inbox; the rest of the app runs without these installed.
google-auth-oauthlib>=1.2.0  # Gmail OAuth consent + token refresh (pulls google-auth)
msal>=1.28.0                 # Microsoft (Outlook) OAuth + token cache

# MCP bridge: lets the claude tier (subscription-billed) reach the mailbox. Only
# imported by mail_mcp.py — a subprocess the Claude CLI spawns — never by the app.
mcp>=1.0
