# Dashboard — Version History

Full feature log for the dashboard, moved out of `dashboard/CLAUDE.md` on 2026-06-22 to keep the always-loaded rule file lean (it carries the run/test commands and behavior rules; this file is reference-only). Check it before adding features that might already exist.

**v1 features (2026-06-10)**
- Stat cards: Active / Overdue / Complete / Total counts
- Filter by status (All / Active / Complete / Archived) and area (Internship / Projects / Research)
- Deadline highlighting: overdue in red, due within 3 days in amber
- Sort by deadline ascending; no-deadline items sink to the bottom
- Live search by title or topic
- File path shown on each card for quick reference

**v1.1 (2026-06-10)**
- Course-aware: `course/<id>` tags surface as a `course` field in `/api/tasks`, an amber chip on each card, and a course filter row (pills are built dynamically from whatever courses exist in the vault — hidden when there are none)

**v1.2 (2026-06-11)**
- Folder gallery on the homepage: a card grid of the top-level sections (projects, research, …), each card showing the folder name, an excerpt from its `_context_*.md` (frontmatter `topic` first, else first body line), and folder/note counts. Clicking a card opens the context file rendered in a modal, with subfolder chips for drill-down, breadcrumb navigation, Esc/backdrop close. Backed by `/api/browse?path=` (vault-rooted, traversal-safe). Open folder persists in the URL as `?folder=`.
- `_context_*.md` and `README*.md` files are excluded from the task list (reference docs and folder stubs, not tasks).

**v1.3 (2026-06-12)**
- Claude token-usage panel: aggregates Claude Code session transcripts from `~/.claude/projects/**/*.jsonl` and surfaces a usage panel under the stat cards. Shows today's tokens, all-time total, cache reads, and an estimated API cost, plus a 14-day token bar chart and a per-model breakdown. Backed by `/api/usage`. The parser reads only token counts, model, and timestamp (never message content), dedupes by `message.id`+`requestId`, and estimates cost from public API list prices (opus/sonnet/haiku families; unknown models count tokens but $0 cost). Renders an empty state when no logs exist.

**v1.4 (2026-06-13)**
- Desktop app shell: `dashboard/desktop.py` (pywebview) wraps the existing Flask app in a native OS window — same code, same UI, no browser tab. Runs Flask in a daemon thread on an OS-assigned free port, waits for it to come up, then opens the window; `desktop.bat` double-click-launches it. External http/https links are routed to the system browser via a small `js_api` bridge (so future web-search result links don't get trapped in the window). The browser path (`python app.py` / `start.bat`) is unchanged and needs no new deps; only the desktop window requires `pywebview`.

**v1.5 (2026-06-13)**
- Real agent loop (workspace stage 3): the stubbed Agents view now runs genuine model-driven, tool-using agents over the vault. `router.py` adds `chat_tools()` — a tier-aware single-step tool-use call (Anthropic native tool use; Ollama tool-calling via message/tool translation). New `agent.py` holds the vault tools (`search_vault`, `read_note`, `list_notes`, `list_folder`, `write_note`, `create_project` — all vault-confined and traversal-safe), an `AGENTS` registry (Daily Summarizer / Project Scaffolder / Research Agent, each = system prompt + allowed tools + default tier), and `run_agent()` which drives the model→tools→results→repeat cycle (step cap 8) emitting a per-step event. `POST /api/agent/run` launches a background-threaded run and returns a `run_id`; `GET /api/agent/run/<id>` polls status + streamed steps (in-memory store, capped). The Agents view streams each step (think / tool call / result) live and renders the final answer. Project-creation logic is now shared via `create_project_core()`. Works on both tiers (local Ollama `fast` and Claude `smart`).

**v1.6 (2026-06-14)**
- Subscription-billed chat: a third router tier, `claude`, shells out to the Claude Code CLI (`claude -p --output-format json`, prompt piped via stdin) so the **Chat** and **Evil Twin** panels run on your Claude subscription instead of API credits. `router.py` adds `_claude_code_chat()` (with `_claude_cli_path()` + `_flatten_conversation()`), a `claude` entry in `status()`, and the tier branch in `chat()`; the subprocess env has `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN` stripped so it always uses subscription (OAuth) auth. Chat's cloud button and the Twin both now default to this tier (cloud pill relabeled "✨ Claude · plan"); `fast` (local Ollama) is unchanged. The **Agents view keeps using the `smart` API tier** — its tool-use loop needs native Anthropic tool calls, which the CLI doesn't expose this way. Configurable via `CLAUDE_CLI`, `CLAUDE_CLI_MODEL` (default: plan default), `CLAUDE_CLI_TIMEOUT`. No new Python deps.

**v1.7 (2026-06-14)**
- Vault-aware local chat: the `fast` (local Ollama) tier in the Chat panel was blind — `/api/chat` called `router.chat()` with no tools, so the weak local model never saw the vault. It now gets the live vault structure injected into its system prompt: `app.py` adds `_vault_outline()` (a hard-capped, indented folder+note tree honoring `IGNORE_DIRS`/dotfile skips, read fresh per request) and `_vault_chat_system()` (preamble + outline + any caller system), applied in `api_chat` only when `tier == "fast"`. The model can now answer "what's in my vault / where does X live" questions grounded in real structure; it's told it can see the structure but not note contents (no tool calls — that's the cloud/agent path). The `smart`/`claude` tiers are untouched (they can read files themselves). No new deps.

**v1.8 (2026-06-14)**
- Home-page daily briefing: a panel under the usage panel aggregating tech-news RSS (HN, TechCrunch, The Verge, Ars Technica), Bluesky founder feeds + blog RSS, and live job openings (Greenhouse/Lever public JSON). Backed by `/api/news`. All fetching is stdlib-only (`urllib` + `xml.etree` + `json`, namespace-agnostic RSS/Atom parser) — no API keys, no new deps. Results are held in an in-process TTL cache (`NEWS_TTL`, default 30 min) — the cache *is* the refresh routine; the first request after expiry refetches, `?refresh=1` forces it. Every source is wrapped so one dead feed degrades to a skipped section (collected in `errors`), never a 500. Sources are configured in `dashboard/news_sources.py` (feed list, Bluesky handles, board slugs). Links open in the system browser in both browser and desktop modes.

**v1.9 (2026-06-14)**
- Hyperstudio dark redesign: the whole UI was re-skinned from the original light theme to the "designer's midnight gallery" dark design system speced in `dashboard/Assets/DESIGN.md` (near-monochrome obsidian canvas `#101010`, flat gray surface stack `#181818`→`#212121`, onyx `#2a2a2a` hairlines, frost text, **Amber Whisper `#e7c59a` as the sole chromatic accent**). All changes live in the single `templates/index.html` — no new endpoints, no Python changes, no build step. The existing CSS referenced design tokens by variable *name* (`--bg`, `--surface`, `--border`, …), so remapping the `:root` block cascaded across every component; only hardcoded light values (error surfaces, modal shadow/backdrop, archived dot, the 7 `color:#fff`-on-accent spots) needed individual edits. Typography moved to **Inter** (Aeonik substitute) + **JetBrains Mono** (Input substitute) via Google Fonts (system fallback offline), body tracking `-0.011em`, and the brand's signature **whisper-weight 400** display type on the big stat/usage numbers and view headings (was bold 700). Header gained the `Regalia_Icon.png` crown-spade logo (served from `/static/logo.png`, also the favicon) + wordmark + mono tagline. Per the system's "no drop shadows" rule, depth now comes from grays + hairlines (gallery card hover lifts to the elevated surface; modal uses a border + deep void backdrop); bright tag chips desaturated to translucent dark; functional status colors (overdue red, due-soon, green dot) kept but muted; dark custom scrollbars; radii on the 8/20/99 system. Active nav/pills/CTAs invert to the white-fill button with `--accent-fg` dark text. All 18 smoke tests pass; no new deps.

**v1.10 (2026-06-14)**
- Tailwind build pipeline (standalone CLI, no Node): added compiled Tailwind v4 so the frontend can adopt utility classes and paste plain-HTML Tailwind components. Uses the **standalone binary** `dashboard/tools/tailwindcss.exe` (~108MB, **gitignored**; re-fetch via `get-tailwind.bat`) — no `npm`/`node_modules`, no new Python dep. `tailwind.input.css` imports only the theme + utilities layers (**preflight intentionally omitted** — its base reset would break the hand-written UI, e.g. markdown list bullets) and declares our Hyperstudio tokens as a v4 `@theme` (`bg-obsidian-canvas`, `text-amber-whisper`, `rounded-cards`, `font-aeonik`, …). Compiles to `static/tailwind.css` (committed, ~5KB minified, served by Flask, linked in `index.html` before the hand-written `<style>`). Build with `build-css.bat` (one-off) or `watch-css.bat` (live). Cascade is safe-by-construction: Tailwind utilities are in `@layer utilities` while the existing `<style>` is unlayered, so unlayered rules always win — utilities can't regress the current look and apply cleanly to new/pasted markup (use `bg-red-500!` to override an existing element). This is the project's first build step (the prior "no build step" rule was relaxed for CSS only, with user sign-off); the Python/Flask path is unchanged. All 18 smoke tests pass. See `dashboard/CLAUDE.md` "Tailwind" for the full workflow.

**v1.11 (2026-06-14)**
- Chat file attachments: the **Chat** panel can now attach images, PDFs, and text files (📎 next to the composer). New `POST /api/chat/upload` validates by extension (images/pdf/txt/md/csv/json/log/py) + size (25 MB), stores each file under a randomized name in `dashboard/.chat_attachments/` (gitignored; inside the vault tree so the `claude` CLI can read it, but under `dashboard/` which the vault walk ignores, so uploads never surface as notes), and returns a handle; old uploads are pruned after 24h. `/api/chat` takes an `attachments` list (handles resolved traversal-safe back to paths via `_resolve_attachments`) and `router.chat()` gained an `attachments` param applied to the latest user turn per tier: the **`claude`** tier is told the file paths and reads them with its own Read tool (enabled non-interactively via `--allowedTools Read` only when files are attached — handles every type); the **`fast`** (Ollama) tier inlines image attachments as base64 for vision models and names non-image files it can't read; the **`smart`** (Anthropic) tier builds native image/document content blocks (parity, not used by the chat panel). Frontend uploads each picked file immediately (staged chips with ⏳→📄, removable), blocks send until uploads finish, allows attachment-only turns, and renders attachment chips on the sent user bubble. Stdlib only (`base64`, `mimetypes`) — no new deps. All 18 smoke tests pass. (Twin reuses the same backend if its UI adds a picker later.)

**v1.12 (2026-06-14)**
- Background beams + liquid glass. An ambient amber "waves" layer and frosted surfaces on the overview. A fixed full-viewport SVG (`.beams`) renders ~9 curved bézier paths with a traveling amber dash (animated `stroke-dashoffset` keyframes — a dependency-free reimplementation of Aceternity's *Background Beams*, recolored to the sole Amber Whisper accent per `Assets/DESIGN.md`), sitting at `z-index:0` behind `.app` (raised to `z-index:1`) so it shows through the transparent canvas gaps; honors `prefers-reduced-motion`. The overview surfaces (stat cards, usage panel + mini-cards, news panel, folder-gallery cards, task cards) became translucent **liquid glass** via new `--glass*` tokens (`rgba` fill + `backdrop-filter: blur+saturate` + lit `--glass-border`), so the beams blur through them. Pure `templates/index.html` CSS — no Python, no Tailwind utilities, no rebuild. All 18 smoke tests pass.

**v1.13 (2026-06-14)**
- Landing page (ASCII hero + parallax), merged as the **entry point**. `/` now opens on a full-viewport hero whose background is a self-contained third-party ASCII-dither + WebGL effect (`dashboard/Assets/ascii_js.js`, copied verbatim to `static/ascii-dither-background.js`; it auto-mounts into `[data-ascii-dither-bg]`) — **the project's first third-party JS asset**, still no build step / no new deps. Below it sits a vanilla port of Aceternity's **Parallax Scroll**: a 3-column grid where columns 1 & 3 drift up and column 2 drifts down, driven by scroll progress mapped to `translate3d` on a `requestAnimationFrame` tick, disabled under `prefers-reduced-motion`. Hero + parallax live **above** the existing dashboard in the one `index.html`; scrolling past them slides the sticky header up and lands in the overview (`#dashboard` anchor). The hero canvases are CSS-overridden to `object-fit:fill` so the WebGL glow fills the hero without edge-clipping on resize **and** the script's linear mouse→grid mapping stays correct (the earlier `cover` cropped the content and offset the hover). No separate route — a brief standalone `/landing` page was folded in and removed. All 18 smoke tests pass.

**v1.14 (2026-06-14)**
- Renamed **Work Vault → Regalia** (branding only — the on-disk folder and `VAULT_ROOT` path are unchanged to avoid breaking git, the vault walk, and the Claude memory store): browser title, header wordmark, desktop window title, docstrings, and `.bat` comments. Hero copy is now a posh greeting ("Salutations."); the logo, tagline, and Enter button were removed. The parallax "in-between" section dropped the marketing copy — it now asks **"What should we work on?"** and lists the folders worked in most recently, backed by new `GET /api/recent-folders` (groups notes by project folder = the first two path segments, so vendored/resource subtrees roll up into their project; ranks by newest note mtime; returns the top 6 with a human "time ago" via a stdlib `_rel_time()` helper + note count). Tiles render into the parallax columns and open that folder in the browse modal on click. The amber beams were trialed on the landing, judged cluttered, and pulled back to the dashboard only (hero + parallax kept opaque). All 18 smoke tests pass.

**v1.15 (2026-06-14)**
- Landing reworked into **pinned scrollytelling** + a **Spotlight**, and the hero title changed to **"Regalia."** (was "Salutations."). The hero + parallax merged into one tall `.scrolly` track whose inner stage **pins** (`position: sticky`) and stays in place while scroll progress drives an *in-place* animation: the title shrinks/fades/rises, then the "What should we work on?" heading + recent-folder tiles assemble over it (staggered), then the stage releases into the dashboard. The earlier parallax-drift script was replaced by one rAF-throttled scroll driver (exposed as `window.__scrollyUpdate` so `renderRecentFolders` re-applies the start state once tiles load); it bails to a static stack under `prefers-reduced-motion` / ≤640px. Added a vanilla port of Aceternity's **Spotlight** — a large skewed ellipse blurred with `feGaussianBlur stdDeviation="151"` that fades + scales in from the top-left, recolored to the amber accent (16% opacity), layered above the ASCII background and below the title (hidden in the static fallback). The ASCII hero background is served from `static/ascii-dither-background.js`, a **copy** of `Assets/ascii_js.js`; new **`dashboard/sync-ascii.bat`** re-copies it after a re-export (the served file doesn't auto-update). All 18 smoke tests pass.

**v1.16 (2026-06-15)**
- Local chat can now read note *contents*, not just the outline. The `fast` (local Ollama) tier of the **Chat** panel was previously grounded only with the vault folder/file outline (v1.7) and was explicitly told it couldn't read note bodies. `/api/chat` now routes the fast tier through a new **read-only tool loop**, `agent.chat_vault()`, which exposes `search_vault` / `read_note` / `list_notes` / `list_folder` (the write/scaffold tools are deliberately withheld — a chat turn must never silently mutate the vault) and drives the model→tools→results cycle (step cap 6) over the full conversation, returning the final prose in `router.chat()`'s shape so it's a drop-in. The vault outline is still injected into that loop's system prompt to target reads. Requires a **tool-capable** local model; if the model can't do tool calls (or any other fast-tier `RouterError`), it gracefully **falls back** to the old outline-only chat. Attachment turns bypass the loop (no `chat_tools` attachment path yet) and keep the image-inlining path. `smart`/`claude` tiers unchanged. All 18 smoke tests pass; no new deps.

**v1.17 (2026-06-15)**
- Chat can now **write to the vault** ("Edit mode"), and the **Smart (Anthropic API) tier** is now selectable in the Chat panel (was fast/claude only). A ✏️ Edit toggle in the chat tier-bar (default off = read-only, unchanged) unlocks writes on **all three tiers**, each by its own path. **fast/smart**: `agent.chat_vault()` gained `allow_write` — when on it adds the existing vault-confined `write_note` / `create_project` tools (`CHAT_WRITE_TOOLS`) to the read-only set plus edit-mode guidance (`CHAT_WRITE_RULES`: read-before-overwrite since `write_note` replaces whole files, act only on explicit requests, report what changed), and bumps the step cap to 8; `/api/chat` routes fast always through the loop and smart through it only when Edit mode is on (otherwise smart takes the plain cloud path). These writes are **`.md`-only, traversal-guarded, vault-confined by construction**. **claude**: `router.chat(allow_write=...)` threads to `_claude_code_chat`, which grants the CLI `--allowedTools Read,Edit,Write,Glob,Grep` + `--permission-mode acceptEdits` (auto-accepts so the headless subprocess never blocks on a permission prompt — there's no terminal to answer one); **Bash is never granted**. The claude path uses real Claude Code `Edit`/`Write` — confined to the vault working dir (`cwd`) but **not** restricted to `.md`, so it's the broader-blast-radius path (git is the safety net). Edit defaults off everywhere; the Twin (also `claude`) is untouched (defaults `allow_write=False`). All 18 smoke tests pass; CLI flags verified; live claude write confirmed; no new deps.

**v1.18 (2026-06-16)**
- Agents on your subscription (no API key): the **Agents view** can now run on the `claude` tier, billed to your signed-in Claude plan instead of API credits. Previously `/api/agent/run` only accepted `fast`/`smart` because the in-process tool loop needs native tool calls the CLI doesn't expose. New `router.claude_code_stream()` runs the Claude Code CLI in streaming mode (`-p --output-format stream-json --verbose`, prompt piped via stdin, `ANTHROPIC_API_KEY`/`AUTH_TOKEN` stripped → OAuth/subscription auth) and **yields each parsed CLI event** (system/assistant/user/result) as the CLI drives its *own* model→tools→repeat loop. New `agent._run_agent_claude()` consumes that stream and maps the CLI's native tool-use/tool-result/text events onto the same step vocabulary the in-process loop emits (start/think/tool/tool_result/final), so **the Agents view streams live regardless of tier** (this was "option 2" — keep the live step view, vs. the one-shot "option 1"). The CLI uses its own file tools (Read/Grep/Glob, plus Write/Edit for agents that write) instead of the custom vault tools, so each agent's system prompt gets a translation note (`_CLAUDE_AGENT_RULES`). Safety: Bash never granted; file access confined to `CLAUDE_CLI_CWD` (vault root); the run is bounded by the `CLAUDE_CLI_TIMEOUT` wall-clock watchdog (Popen streaming has no built-in timeout, and the installed CLI has no `--max-turns`). Frontend: each agent card gained a tier `<select>` (fast/smart/claude, defaulting to the agent's registry tier) + an amber `.tier.claude` chip; `/api/agent/run` accepts `claude`. **21 smoke tests pass** (+3 offline tests stubbing `claude_code_stream`); live claude-tier run confirmed (summarizer via Glob); no new deps.

**v1.19 (2026-06-19)**
- Multi-inbox email (Gmail + Outlook): read inboxes and save drafts. A new **Inbox** sidebar view lists connected accounts with unread counts, shows recent/searched messages in a list + reading pane, and has a Compose / Draft-reply modal whose button says "Save draft" (drafts are **never sent** — you review and send in your mail client). New modules: `email_sources.py` (config only — provider REST endpoints, OAuth scopes, fetch limits/cache TTL; mirrors `news_sources.py`), `mailbox.py` (the engine: gitignored per-account token store, token refresh, `fetch_inbox`/`read_message`/`search_messages`/`create_draft`, provider-agnostic shape across Gmail + Graph, in-process TTL cache + graceful per-account degradation like the briefing), and `connect_email.py` (one-time interactive OAuth consent CLI). Endpoints: `GET /api/inboxes`, `GET /api/inbox/<account_id>`, `GET /api/email/<account_id>/<msg_id>`, `POST /api/email/draft`. `agent.py` gained email tools (`list_inboxes`/`read_inbox`/`search_email`/`read_email` read-only + `draft_email`, gated like the write tools — never in read-only `CHAT_TOOLS`) and an **Inbox Triage** agent (tier `smart`). **Auth approach #2.5:** auth-only libs (`google-auth-oauthlib` + `msal`, lazily imported) handle the OAuth/token dance; mail I/O is stdlib `urllib`+`json` against the Gmail API + Microsoft Graph — this is the project's first relaxation of the no-new-deps rule (user-approved), and the rest of the app still runs without these installed. **Drafts-only is structural** (no send fn; Graph token lacks `Mail.Send`; Gmail send endpoint never called). Tokens never logged/returned; account ids traversal-guarded into `.email_tokens/` (gitignored). 35 smoke tests pass (+14 email tests: endpoint shapes, drafts-only guarantees, secret hygiene, token-store traversal). See the "Email" note in `CLAUDE.md`.

**v1.20 (2026-07-09)**
- Multi-chat (concurrent sessions): the **Chat** view went from one throwaway conversation to a Claude-Desktop-style multi-session cockpit — a conversation **sidebar** (＋ New chat, per-chat delete ×, auto-derived titles from the first user message), **server-side persistence** (conversations survive reload and app restart), and **true concurrent generation** (send in chat A, switch to chat B and send there too; A keeps generating and lands with a green "new reply" dot on its sidebar entry). New `chats.py` store module — one JSON file per conversation in the gitignored `dashboard/.chats/` (`{id, title, created, updated, tier, model, edit, messages}`), ids minted as `c_<uuid4hex>` and regex-validated before any disk touch (traversal-guarded like `mailbox._account_path`). New CRUD routes `GET/POST /api/chats` + `GET/PUT/DELETE /api/chats/<cid>` (PUT treats the URL id as authoritative so a body id can't redirect the write); **`/api/chat` generation is untouched — still stateless** — the concurrency comes purely from the frontend refactor: the module-level singletons (`chatMsgs`/`chatBusy`/`chatTier`/`chatModel`/`chatEdit`/`chatAttachments`) became a per-conversation map (`chats{}` / `chatOrder[]` / `activeChat`), each send closure captures its conversation id and only touches the DOM when that conversation is visible, and a new `renderChatLog()` rebuilds the log from state (rendering was append-only before — switching was impossible). Tier/model/Edit-mode are now remembered **per conversation** (persisted on change); attachments stage per conversation; Clear empties the active chat's messages while × deletes a chat. Transcripts store attachment display metadata only (`{name,mime}`) — the 24h-pruned `.chat_attachments/` lifecycle is unchanged. Sidebar is hand-written CSS (no Tailwind rebuild); collapses to a horizontal strip ≤720px. Twin unchanged (single conversation). **50 smoke tests pass** (+5: CRUD round-trip, list shape, 404s, bad-id rejection incl. through the route, body-id-redirect guard — store pointed at a temp dir); live two-chat concurrent run verified in-browser (background reply + done dot + switch + reload persistence, files inspected on disk); no new deps.

**v1.21 (2026-07-09)**
- **Hermes revert:** the partially-built Hermes agent-runtime switch (a `hermes` router tier, a one-shot `_run_agent_hermes()` runner, a `MAILBOX_BACKEND=hermes` retrieval path in `mailbox.py`, and `HERMES_*`/`MAILBOX_BACKEND` config) was **removed in full**, restoring the original minimal-dependency design — the inbox manager is the home-built v1.19 OAuth engine again (Gmail REST + Graph over stdlib `urllib`, drafts-only by construction), and `researcher` + `inbox_triage` default back to the in-process `smart` tier. `HERMES_PLAN.md` deleted; decision record in `_context_dashboard.md`. 40 smoke tests pass (−10 Hermes retrieval tests).

**v1.22 (2026-07-10)**
- **MCP mailbox bridge — Inbox Triage on the Claude subscription (no API cost):** the `claude` CLI tier can now reach the email engine. New `mail_mcp.py` — a stdio MCP server (official `mcp` SDK, FastMCP) exposing the five email tools (`list_inboxes`/`read_inbox`/`search_email`/`read_email`/`draft_email`) as thin wrappers over the same `agent._tool_*` functions the in-process loop uses, so output is identical on every tier; deliberately **no send tool**. `router.claude_code_stream()` gained an `mcp_config` param (`--mcp-config <file> --strict-mcp-config`); `agent._run_agent_claude()` is now **tool-category-aware** — vault tools grant the CLI's native Read/Grep/Glob(+Write/Edit) with `_CLAUDE_AGENT_RULES`, email tools write a per-run temp MCP config (`_mailbox_mcp_config()`, `sys.executable` + abs path, deleted in `finally`) and grant only `mcp__mailbox__<tool>` with new `_CLAUDE_EMAIL_RULES` — an email-only agent gets **no filesystem tools at all**. **Inbox Triage's default tier flipped `smart` → `claude`** (subscription-billed; `smart` stays selectable). New dep `mcp>=1.0`, imported only by the `mail_mcp.py` subprocess, never by the app. Verified: standalone MCP handshake (initialize / tools/list / live `tools/call list_inboxes` returning both connected Gmail accounts).

**v1.23 (2026-07-10)**
- **Important-mail panel replaces Social & blogs:** the overview briefing's middle column now shows **work/school-relevant mail from the last 2 days** across all connected inboxes instead of Bluesky/blog posts. New `mailbox.important_messages()` — pulls `IMPORTANT_DAYS` of mail per account (Gmail narrows server-side with `newer_than:`, Graph is date-filtered client-side), scores each message with a **deterministic** keyword/domain/penalty heuristic (`email_sources.py` config: `IMPORTANT_KEYWORDS`/`IMPORTANT_SENDER_DOMAINS`/`IMPORTANT_PENALTIES`, no model call on the page-load path), and returns the top `IMPORTANT_MAX_SHOWN` sorted by score then recency; helpers `_importance_score` (pure), `_parse_msg_date` (RFC 2822 + ISO), `_fmt_when`, `_display_name`. New route `GET /api/mail/important` (deliberately not under `/api/inbox/…` — that segment is the dynamic `<account_id>` route); degrades to `available:false` with no inbox, per-account errors never 500. Frontend: the briefing's social column became `#mail-col`, filled by a separate fetch so slow mail never blocks news; rows show unread dot + subject (hover: matched signals; click → Inbox view) + sender/time/account. The Bluesky/blog fetch (`_fetch_bluesky`, `BLUESKY_HANDLES`, `BLOG_FEEDS`) was retired. Reuses `fetch_inbox`'s TTL cache. 55 smoke tests pass (+5: scorer boosts/penalties, date parsing, filter/sort, no-account degrade).

**v1.24 (2026-07-17) — public release: Settings, OpenAI tier, in-app connections, installer**
- **Settings view + config store:** new `config.py` — a tiny JSON store (gitignored `.config.json`, atomic writes, unknown-key rejection) behind `GET/POST /api/settings`; secrets are **never returned** (masked to `{"set": bool}`). New ⚙️ Settings view: theme (dark / new **light** palette — a full `:root[data-theme="light"]` token set), accent color (drives `--amber*`), default chat tier, landing-page on/off (`.no-landing` + scrolly-driver bail), vault folder. Boot settings are server-injected into `index.html` pre-paint (no theme flash).
- **Configurable vault root:** `REGALIA_VAULT` env or the Settings `vault_path` override where the vault lives (was: hardwired to the repo root); read at startup by `app.py` + `router.py` (CLI cwd follows).
- **`openai` router tier (ChatGPT):** fourth tier over plain REST/urllib (no SDK dep) — `_openai_chat`/`_openai_chat_tools` with canonical-history translation (tool_calls ids + JSON-string args, `role:"tool"` results, image data-URIs), model `OPENAI_MODEL` (default `gpt-5.6-terra`), branches in `chat()`/`chat_tools()`/`status()`, pills/selects in Chat + Agents + Settings. Edit-mode chat and agents run on it via the in-process tool loop.
- **API keys in-app:** `config.secret()` — env wins, then the store — feeds Anthropic + OpenAI clients; keys entered in Settings → Connections light the tiers up without a restart.
- **In-app inbox connect:** `POST /api/connect/email` runs the existing `connect_email` OAuth flow on a background thread (pops the system browser; single-flight lock), `GET /api/connect/status` reports progress; Connections panel lists inboxes + backend status dots. Scopes untouched — still **drafts-only by construction**.
- **Installer/distribution:** `regalia.spec` (PyInstaller, onedir, bundles templates/static/Assets) + `build.bat`/`build.sh`; new `paths.py` moves per-user state (config/chats/tokens/uploads) to the OS data dir when frozen, vault defaults to `~/RegaliaVault`. GitHub Actions: `tests.yml` (smoke suite on every push/PR) and `release.yml` (tag `v*` → Windows + macOS builds attached to the Release). Repo-level: root `README.md` (product landing), MIT `LICENSE`, `.env.example`, `RELEASE.md` (history-purge runbook + release steps), PII/employer scrub of the public skeleton.
- 61 smoke tests pass (+11: settings round-trip/validation/masking, default-tier inheritance, openai status/key/availability, connect-route validation).

**v1.25 (2026-07-17) — everything configurable from the UI**
- **Every backend knob became a Settings field** (env var of the same name still wins; empty = default): Ollama host + model, Anthropic model, OpenAI model + base URL, Claude CLI path/model/timeout, briefing cache TTL, and the email OAuth client creds. `config.py` grew the keys plus a general `value(key, env_var, default)` resolver (env → store → default) and int/URL validation; `router.py`'s module-level constants became per-call accessors (`_ollama_host()`, `_openai_model()`, …) so a Settings change applies to the **next request, no restart**. `CLAUDE_CLI_CWD` and `ANTHROPIC_AUTH_TOKEN` stay env-only on purpose (safety boundaries).
- **Gmail OAuth client — paste, don't path:** Settings → Email has a textarea for the downloaded `client_secret.json` (validated: must be JSON with a top-level `installed`/`web` key; stored as a **secret**, masked to `{"set": bool}`). `email_sources.gmail_client_secret_file()` resolves env path → pasted JSON (materialized to a 0600 file under the data dir) so `connect_email`'s `from_client_secrets_file` works unchanged. Outlook client id + tenant are plain Settings fields (datalist: consumers/organizations/common, free text for a tenant GUID); `ms_authority()` computed lazily. Connect guards (`connect_email.py`, `mailbox._ms_token`) now check the config-aware accessors and their errors point at Settings, not env vars.
- **In-app Ollama model pull:** `POST /api/ollama/pull` streams Ollama's `/api/pull` JSON-lines progress on a background thread (single-flight, model-name regex-validated), `GET /api/ollama/pull/status` polled by the UI — a ⬇ Pull button with live % next to a model box fed by a `/api/ollama/models` datalist, plus an install link when the dot is red.
- **Claude CLI connection test:** `POST /api/claude/test` runs a tiny prompt through the CLI in the background (`GET /api/claude/test/status` polls) — proves *signed in*, not just on PATH; the failure detail tells the user to sign in in a terminal. CLI path/model/timeout are Settings fields with an install link.
- **Settings view reorganized into sections** — Appearance / Defaults / Advanced (vault path, news TTL) / Models & backends (per-tier cards with status dots + effective-model placeholders from `/api/router/status`) / Email (guided one-time OAuth app setup in a collapsible, then Connect buttons). The two stale empty-states that said "run `python connect_email.py`" became buttons into Settings; a test now asserts no UI string mentions `connect_email.py` or `GMAIL_OAUTH_CLIENT`.
- 69 smoke tests pass (+8: knob round-trip, int/URL validation, gmail-JSON validation/masking/materialization, env-over-store precedence, pull validation, test-route shape, no-CLI-instructions guard).

**v1.26 (2026-07-17) — dynamic project areas**
- **Areas come from the vault, not a hardcoded trio:** on a fresh install (`~/RegaliaVault` is created empty) the Projects view, new-project form, and task filters no longer show the developer's personal taxonomy (`projects` / `Internship-Projects` / `research`). `app.py` dropped `AREA_DIRS` for `_safe_area()` — any sane top-level folder name is a valid area (charset-gated `^[A-Za-z0-9][A-Za-z0-9 _-]{0,63}$`, so traversal is impossible by construction; `IGNORE_DIRS` names rejected; an existing top-level folder is reused case-insensitively so `projects/` and `Projects/` never coexist), created on demand by the scaffolder. `_area_tag()` derives the frontmatter `area/<tag>` (lowercase-hyphenated folder name); legacy continuity: the `internship` alias still lands in `Internship-Projects/` and keeps tagging `area/internship`.
- **Projects view groups by real top-level folders:** `loadProjects()` walks `/api/browse` root and renders each folder with subfolders as a group (full-row `.proj-group-head` header + the existing cards); loose-note folders are skipped. Empty vault → a clean empty state pointing at the form. The form's Area `<select>` is populated from the same fetch, with a "New area…" free-text option — prefilled `projects` when the vault is empty.
- **Area filter pills are dynamic:** `buildAreaPills()` (mirrors `buildCoursePills`) derives pills from the `area/<x>` tags actually present in `/api/tasks`; hidden when none, stale selections reset to All. Area tag chips are colored from a 3-color palette by a stable name hash (the old per-area CSS classes are gone, and the tag text is now escaped).
- **Agent text updated:** `create_project`'s `area` schema, the Scaffolder prompt, and `_CLAUDE_AGENT_RULES` now say "reuse a fitting top-level folder, else a short new name (default `projects`)" instead of naming the trio.
- 79 smoke tests pass (+8 `ProjectScaffoldTests` against a temp vault: empty-vault create, custom area + slug tag, internship alias, case-insensitive reuse, pre-write rejection of traversal/reserved names, duplicate → 409, route shapes, browse reflects the new area).

**v1.27 (2026-07-17) — packaged mailbox MCP, ChatGPT-auth gating, store/UI hardening**
- **The mailbox MCP works in packaged builds:** `regalia.spec` previously carried `excludes=["mcp"]` on the reasoning that only the CLI-spawned `mail_mcp.py` subprocess needed it — but a frozen build has no interpreter to run that script, so the `claude`-tier email path (Inbox Triage) could not attach its MCP server at all. The exe is now its own MCP launcher: `desktop.py` gains a `_run_frozen_mail_mcp_entrypoint()` that runs before the pywebview import and, when `sys.frozen` and `argv == ["--mail-mcp"]`, serves `mail_mcp.mcp` and exits. `agent._mailbox_mcp_config()` branches on `sys.frozen` to spawn `sys.executable --mail-mcp` instead of `python mail_mcp.py`. `mcp` moved from `excludes` to `hiddenimports` (with `mail_mcp` and `mcp.server.fastmcp`); the import stays inside the MCP branch so a normal launch never initializes the server or its SDK.
- **`chatgpt` readiness means authenticated, not installed:** `status()` reported `available` from executable presence alone, so a logged-out or API-key-mode Codex CLI looked live. `status_for(tier)` now reports `installed`/`authenticated`/`available` separately for `chatgpt`, backed by a `_CODEX_HEALTH` cache **keyed by the resolved executable** (changing the CLI setting cannot leave stale auth state on a different binary). `codex_cli_health()` treats an API-key login as *not ready* (the tier exists to bill the ChatGPT account) and every `_codex_exec_chat()` outcome refreshes the cache. Runs pass `forced_login_method="chatgpt"`, `--ignore-user-config`, and `--ignore-rules` so a user's Codex config cannot redirect the tier. (`--ask-for-approval` was removed: headless `codex exec` hardcodes it to Never and the flag is a parser error.)
- **`status()` split so one tier's check stops probing every backend:** `status_for()` lets `/api/router/check` evaluate just the requested tier — a Codex check no longer fires an Ollama HTTP probe. The Settings UI reuses the status already carried in the check response instead of issuing two more full status requests.
- **Codex subprocess env is allowlisted, not filtered:** the old env stripped only `CODEX_API_KEY`/`OPENAI_API_KEY` and passed everything else. Edit-mode Codex can run shell commands in its sandbox, so unrelated cloud/database keys in the dashboard's environment were reachable by model-visible commands. `_codex_env()` now passes only `_CODEX_ENV_KEYS` (discovery, per-user auth store, temp/locale/proxy/CA), compared case-insensitively for Windows. Attachments are staged into a per-run `TemporaryDirectory` granted via `--add-dir` and removed after the turn, so the CLI never gets write access to the upload source.
- **Transcript writes are atomic:** `chats.py` wrote with `path.write_text()`, so a concurrent read could observe partial JSON. `_write_chat()` writes to a temp file in the store and `os.replace()`s it under a module lock. `_chat_path()` no longer resolves the target file (on Windows that can fail while another thread replaces it) — the strict `c_<32 hex>` id regex is what makes the child path safe.
- **Chat persistence is ordered and delete is final:** the UI fired PUTs fire-and-forget, so an older save could land after a newer one. Saves are now serialized per conversation (`chatSaveQueues`), and a `deletedChats` tombstone makes every queued PUT skip — an in-flight save can no longer resurrect a deleted transcript. An active PUT is deliberately *not* aborted (client abort doesn't prove the server stopped).
- **Bad payloads fail loudly instead of silently degrading:** `/api/chat`, `/api/agent/run`, and `/api/router/check` rejected non-object JSON via `or {}` and fell through to confusing errors; they now return 400. An unknown chat tier is a 400 rather than a silent fallback. Agent tiers are gated by the new `agent.AGENT_TIERS` (`fast`/`smart`/`openai`/`claude`) — agent runs need tool calling, which the ChatGPT-account backend doesn't expose — so `openai` is no longer silently dropped and `chatgpt` returns an explicit error naming the usable tiers. `config.update()` bounds the interval knobs to 1–86400s.
- 87 smoke tests pass (+8: concurrent-save integrity, API-key login rejected, per-tier checks that don't probe other backends, `openai` agent tier preserved, unsupported tier is an explicit error, non-object JSON rejected, unknown tier not silently downgraded).

## Planned / backlog
- Click a card to open the note in the default editor
- Inline status toggle (mark complete without leaving the dashboard)
- Create new task/note from dashboard (writes a file with correct frontmatter)
- Group-by view (group by area or type instead of flat sorted list)
- Tag-based filtering beyond area (type, course, etc.)
- Persistent filter state (URL params or localStorage)
